
$(document).ready(function(){
	$(".project").hover(function(){
		// Clone project image
		// Append to current div
		// Animate proportions
		// Slide up text
	},function(){
		// Slide down text
		// Animate back to original proportions
		// Remove appended div
	});
});
